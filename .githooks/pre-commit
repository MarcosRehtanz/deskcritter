#!/bin/bash
# Bloquear commits directos en la rama main

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ]; then
  echo "❌ No se permite commitear directamente en 'main'."
  echo "   Crea una rama con: git checkout -b mi-rama"
  exit 1
fi
