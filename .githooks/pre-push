#!/bin/bash
# Bloquear push directo a la rama main

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ]; then
  echo "❌ No se permite pushear directamente a 'main'."
  echo "   Usa un Pull Request para integrar cambios."
  exit 1
fi
